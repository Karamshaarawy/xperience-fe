"use client";

import { useEffect } from "react";
import isAuth from "../../../components/isAuth";

function Dashboard() {
  useEffect(() => {}, []);

  return <div></div>;
}

export default isAuth(Dashboard);
